# Install in a separate inference environment when stable PyPI vLLM does not
# yet contain Qwen3.5 support:
# uv pip install vllm --torch-backend=auto \
#   --extra-index-url https://wheels.vllm.ai/nightly
vllm
openai>=1.90.0
